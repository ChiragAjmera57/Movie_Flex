let initstate = {
    url:{},
    trending:[],
    popular:[],
    toprated:[]
}
export const homereducer = (state=initstate,{type,paylod}) => {

    switch (type) {
        
        default:
            return state
    }
}